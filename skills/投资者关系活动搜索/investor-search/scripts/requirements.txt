# investor搜索技能依赖
# 使用基础包官方库和常用库，尽量少依赖第三方库

# 核心依赖
requests>=2.25.0
python-dateutil>=2.8.0

# 可选依赖（用于数据处理）
pandas>=1.3.0  # 用于CSV文件处理
tabulate>=0.8.0  # 用于表格输出

# 开发依赖
pytest>=6.0.0
black>=21.0.0
flake8>=3.9.0